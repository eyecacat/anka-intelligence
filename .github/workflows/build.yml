<<<<<<< HEAD
=======
name: Build Anka Intelligence ISO
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

>>>>>>> ab29af2 (fix: enforce debian mode in build script and isolate build in docker)
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build ISO inside Debian Docker Container
        run: |
<<<<<<< HEAD
          docker run --rm --privileged \
            -v "$PWD":/work \
            -w /work \
            debian:bookworm \
            bash -c "
              apt-get update && apt-get install -y live-build debootstrap debian-archive-keyring ca-certificates squashfs-tools xorriso isolinux syslinux-common
              bash build.sh
            "
=======
          docker run --rm --privileged             -v "$PWD":/work             -w /work             debian:bookworm             bash -c "
              apt-get update && apt-get install -y live-build debootstrap debian-archive-keyring ca-certificates squashfs-tools xorriso isolinux syslinux-common
              bash build.sh
            "

      - name: Upload ISO to Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: build/*.iso
>>>>>>> ab29af2 (fix: enforce debian mode in build script and isolate build in docker)
